{
  "name": "dan-mcp-server",
  "version": "1.0.0",
  "description": "HTTP/SSE proxy that wraps the Relevance AI MCP server for use in agents",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "@relevanceai/relevanceai-mcp-server": "latest",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
